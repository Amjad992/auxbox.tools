export default function HeroSection() {
  return (
    <section className="calculator-hero">
      <h1>Salary Raise Calculator</h1>
      <p className="calculator-tagline">
        Plug in your pay, see exactly what a raise looks like across hourly,
        weekly, monthly, and annual.
      </p>
    </section>
  );
}
